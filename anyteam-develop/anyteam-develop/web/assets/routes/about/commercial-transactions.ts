import { createApp } from 'vue';
import CommercialTransactions from '../../pages/about/CommercialTransactions.vue';

window.addEventListener('DOMContentLoaded', () => {
  createApp(CommercialTransactions).mount('#app')
});
