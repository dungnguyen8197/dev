import { HelperCreateApp } from '../../helpers/common';
import SupportReward from '../../pages/user/SupportReward.vue';

window.addEventListener('DOMContentLoaded', () => {
  HelperCreateApp(SupportReward);
});
